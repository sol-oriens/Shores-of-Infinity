namespace CommunityPatch {
	//Developer Note:
	//These constants change how the Community Patch version is displayed, if at all
	//This is used both to check that the CP is correctly applied and its version
	//Both or neither can be enabled, default will only show a discreet console message
	bool showVersionCheckLabel = true;
	bool showVersionCheckConsole = false;
}
